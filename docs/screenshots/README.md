# Screenshots

Generated by `node e2e/screenshots.mjs` from the exported web bundle, de-DE, 414×896@2x. Regenerate after a UI change; do not edit by hand.

## The empty roll list

First launch: no roll yet, one button to create one.

![The empty roll list (light)](light/01-rolls-empty.png)
![The empty roll list (dark)](dark/01-rolls-empty.png)

## New roll from a film preset

Picking Kodak Gold 200 fills in ISO 200 and 36 exposures; the camera comes from the seeded Minolta kit.

![New roll from a film preset (light)](light/02-roll-new.png)
![New roll from a film preset (dark)](dark/02-roll-new.png)

## The roll, still empty

Film, camera and ISO are on the roll; the frame list starts at 0 / 36.

![The roll, still empty (light)](light/03-roll-detail-empty.png)
![The roll, still empty (dark)](dark/03-roll-detail-empty.png)

## A new frame starts from the camera defaults

Program mode, the 35-70 zoom, the UV filter and hand-held support are prefilled - in the field only what actually changed has to be touched.

![A new frame starts from the camera defaults (light)](light/04-frame-defaults.png)
![A new frame starts from the camera defaults (dark)](dark/04-frame-defaults.png)

## Recording the exposure

Manual mode opens the shutter and aperture fields: 1/125 at f/8.

![Recording the exposure (light)](light/05-frame-exposure.png)
![Recording the exposure (dark)](dark/05-frame-exposure.png)

## Edge case: the shutter is slower than the lens allows hand-held

At 1/15 hand-held the domain rules warn about camera shake. Warnings explain, they do not block saving.

![Edge case: the shutter is slower than the lens allows hand-held (light)](light/06-frame-shake-warning.png)
![Edge case: the shutter is slower than the lens allows hand-held (dark)](dark/06-frame-shake-warning.png)

## Edge case: a linear polarizer defeats the autofocus

With the 50 mm and the linear PL filter mounted, the rules point out that autofocus will not work - focus manually.

![Edge case: a linear polarizer defeats the autofocus (light)](light/07-frame-polarizer-af.png)
![Edge case: a linear polarizer defeats the autofocus (dark)](dark/07-frame-polarizer-af.png)

## Edge case: bulb outside manual mode

The Minolta offers B only in M; picking it in P is reported straight away.

![Edge case: bulb outside manual mode (light)](light/08-frame-bulb-outside-m.png)
![Edge case: bulb outside manual mode (dark)](dark/08-frame-bulb-outside-m.png)

## The roll counts the frame

Back on the roll: 1 / 36 frames, the newest first.

![The roll counts the frame (light)](light/09-roll-progress.png)
![The roll counts the frame (dark)](dark/09-roll-progress.png)

## Edge case: scan import without a server

Scans live in PocketBase, so the import asks for the server connection first instead of failing halfway through an upload.

![Edge case: scan import without a server (light)](light/10-scan-import-without-server.png)
![Edge case: scan import without a server (dark)](dark/10-scan-import-without-server.png)

## The equipment tab

The seeded Minolta kit: body, three lenses, filters, flash - all editable.

![The equipment tab (light)](light/11-equipment.png)
![The equipment tab (dark)](dark/11-equipment.png)

## Settings

Language, server, WordPress and the reset - nothing else needs configuring.

![Settings (light)](light/12-settings.png)
![Settings (dark)](dark/12-settings.png)

## The server connection

Offline by default: the app is fully usable without this screen. Sync, scans and export are what need it.

![The server connection (light)](light/13-server-settings.png)
![The server connection (dark)](dark/13-server-settings.png)
