{
  "name": "@filmnotes/backend",
  "private": true,
  "version": "0.2.0-alpha.0",
  "description": "PocketBase backend for filmnotes: versioned schema, owner-scoped API rules, Docker deployment",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "fetch-pb": "node scripts/fetch-pocketbase.mjs",
    "start:dev": "bin/pocketbase serve --dev --dir pb_data --migrationsDir pb_migrations",
    "test": "node --test \"test/*.test.mjs\""
  },
  "devDependencies": {
    "fflate": "^0.8.2"
  }
}
