/**
 * The i18next namespace of the sync screens.
 *
 * The strings sit in `sync.<language>.json` next to this file; `src/i18n/resources.ts` wires them
 * up. Modules of this feature import the constant and call `useTranslation(SYNC_NAMESPACE)`. */
export const SYNC_NAMESPACE = "sync";
