/**
 * The i18next namespace of the scan import screen.
 *
 * The strings sit in `scans.<language>.json` next to this file; `src/i18n/resources.ts` wires them
 * up. Modules of this feature import the constant and call `useTranslation(SCANS_NAMESPACE)`. */
export const SCANS_NAMESPACE = "scans";
