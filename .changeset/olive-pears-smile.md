---
"@filmnotes/backend": patch
---

The Compose project is named `filmnotes`, so the data volume is `filmnotes_pb_data` wherever the
checkout lives. The backup and restore commands in `docs/deployment.md` are literal again — before,
they only matched a checkout in a directory called `filmnotes`.
