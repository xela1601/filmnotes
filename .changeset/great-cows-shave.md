---
"@filmnotes/mobile": patch
---

An export target that is not set up is now named in its own hint ("WordPress-Entwurf ist nicht
eingerichtet."), and a target that needs no settings is simply offered. Adding a new target no
longer means editing both export screens.
