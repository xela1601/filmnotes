---
"@filmnotes/scan-import": patch
---

The desktop import now refuses a file the server does not accept — HEIC above all — and says so
per file, instead of uploading it and leaving a scan record behind that the server rejects. The
app already did this; both now read the same list from the domain.
